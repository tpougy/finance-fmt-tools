---
phase: 01-format-engine-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FinanceFmtTools.sln
  - src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj
  - src/FinanceFmtTools.Engine/FormatKeys.cs
  - src/FinanceFmtTools.Engine/FormatCategory.cs
  - src/FinanceFmtTools.Engine/CellAlignment.cs
  - src/FinanceFmtTools.Engine/FormatDef.cs
  - src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj
autonomous: true
requirements: [DEV-01]

must_haves:
  truths:
    - "Running `dotnet build src/FinanceFmtTools.sln -c Release` succeeds with 0 Warning(s)/0 Error(s), on a machine with only the .NET 8 SDK installed (no Visual Studio, no Windows/.NET Framework runtime required for the build step)."
    - "Running `dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj -c Release` executes successfully (0 tests is an acceptable outcome at this stage — later plans add the actual test cases)."
  artifacts:
    - path: "src/FinanceFmtTools.sln"
      provides: "Solution file referencing both projects"
    - path: "src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj"
      provides: "Multi-target class library (net48;net8.0), zero runtime dependencies except the conditional net48 reference-assemblies package"
      contains: "<TargetFrameworks>net48;net8.0</TargetFrameworks>"
    - path: "src/FinanceFmtTools.Engine/FormatKeys.cs"
      provides: "11 format-key string constants mirroring modConfig.bas FMT_* constants"
    - path: "src/FinanceFmtTools.Engine/FormatDef.cs"
      provides: "Immutable value type for a resolved format (Key, DisplayName, NumberFormat, Category, Alignment)"
    - path: "src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj"
      provides: "net8.0-only xUnit test project referencing the engine via ProjectReference"
      contains: "<TargetFramework>net8.0</TargetFramework>"
  key_links:
    - from: "src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj"
      to: "src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"\\.\\.\\\\FinanceFmtTools\\.Engine"
---

<objective>
Bootstrap the two-project C# solution (`FinanceFmtTools.Engine` class library + `FinanceFmtTools.Engine.Tests` xUnit project) using only the `dotnet` CLI, and define the shared, COM-free contract types (`FormatKeys`, `FormatCategory`, `CellAlignment`, `FormatDef`) that the format-logic tasks in later plans (01-02, 01-03) will implement against.

Purpose: Establishes the empirically-verified build/test layout from 01-RESEARCH.md (class library multi-targeting `net48;net8.0` + a `net8.0`-only xUnit test project via `ProjectReference`) so `dotnet build`/`dotnet test` work end-to-end on this Linux dev machine (DEV-01), and hands downstream plans ready-made contracts so they implement logic, not scaffolding.
Output: `src/FinanceFmtTools.sln`, both `.csproj` files building cleanly, and 4 new C# contract files with zero Excel/COM references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-format-engine-core/01-RESEARCH.md
@src/modConfig.bas
@src/modFormatEngine.bas

<interfaces>
<!-- Exact values the executor must port 1:1 from the VBA source of truth. No exploration needed. -->

From src/modConfig.bas:19-29 (FMT_* constants — port as string VALUES, not just names):
```
FMT_FIN_8D       = "FIN_8D"
FMT_FIN_4D       = "FIN_4D"
FMT_FIN_2D       = "FIN_2D"
FMT_PCT_4D       = "PCT_4D"
FMT_PCT_2D       = "PCT_2D"
FMT_SPREAD_BPS   = "SPREAD_BPS"
FMT_DATE_ISO     = "DATE_ISO"
FMT_DATE_BR      = "DATE_BR"
FMT_DATE_BR_LONG = "DATE_BR_LONG"
FMT_TEXT         = "TEXT"
FMT_INTEGER      = "INTEGER"
```

From src/modFormatEngine.bas:9-16 (FormatDef Type — the C# `FormatDef` class must expose the same 5 fields, typed idiomatically):
```
Public Type FormatDef
    key          As String
    DisplayName  As String
    NumberFmt    As String
    Category     As String      ' "numeric", "percent", "date", "text"
    Alignment    As XlHAlign    ' xlHAlignRight, xlHAlignLeft, xlHAlignGeneral
End Type
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap solution and multi-target Engine class library</name>
  <files>src/FinanceFmtTools.sln, src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj</files>
  <read_first>
    .planning/phases/01-format-engine-core/01-RESEARCH.md (Architecture Patterns > Pattern 1 — exact csproj shape empirically verified this session; Environment Availability — dotnet SDK install fallback)
    CLAUDE.md (Constraints: .NET Framework 4.8 shipping target, buildable with .NET 8 SDK, VS Code + dotnet CLI only)
  </read_first>
  <action>
    Confirm the .NET 8 SDK is on PATH by running `dotnet --version`. If missing, install it via the official script exactly as verified in 01-RESEARCH.md: `curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 8.0 --install-dir "$HOME/.dotnet"` then add `$HOME/.dotnet` to `PATH` for the current shell.
    From the repo root, run `dotnet new sln -n FinanceFmtTools -o src` to create `src/FinanceFmtTools.sln`.
    Run `dotnet new classlib -n FinanceFmtTools.Engine -o src/FinanceFmtTools.Engine --framework net8.0`, then delete the generated `src/FinanceFmtTools.Engine/Class1.cs`.
    Edit `src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj`: replace the singular `<TargetFramework>net8.0</TargetFramework>` with `<TargetFrameworks>net48;net8.0</TargetFrameworks>` (plural). Add `<LangVersion>9.0</LangVersion>` and `<Nullable>disable</Nullable>` to the same `<PropertyGroup>`. Remove or set `<ImplicitUsings>disable</ImplicitUsings>` — the net8.0 template default (`ImplicitUsings=enable`) requires global-usings support that is incompatible with `LangVersion 9.0` on the `net48` leg and must be turned off for both TFMs to keep the two legs behaviorally identical.
    Add a conditional package reference for the `net48` leg only, so the project can compile against .NET Framework reference assemblies without Windows installed: `<ItemGroup Condition="'$(TargetFramework)' == 'net48'"><PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies" Version="1.0.3"><PrivateAssets>all</PrivateAssets><IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets></PackageReference></ItemGroup>`.
    Run `dotnet sln src/FinanceFmtTools.sln add src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj`.
    Build to confirm both TFMs compile cleanly: `dotnet build src/FinanceFmtTools.sln -c Release`.
  </action>
  <verify>
    <automated>dotnet build src/FinanceFmtTools.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/FinanceFmtTools.sln -c Release` exits with status 0.
    - Build output contains the literal substring "0 Warning(s)" and "0 Error(s)".
    - File `src/FinanceFmtTools.Engine/bin/Release/net48/FinanceFmtTools.Engine.dll` exists after build.
    - File `src/FinanceFmtTools.Engine/bin/Release/net8.0/FinanceFmtTools.Engine.dll` exists after build.
    - `src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj` contains the exact string `<TargetFrameworks>net48;net8.0</TargetFrameworks>`.
    - `src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj` contains `Microsoft.NETFramework.ReferenceAssemblies` scoped inside an `ItemGroup` conditioned on `'$(TargetFramework)' == 'net48'`.
    - No file under `src/FinanceFmtTools.Engine/` references `Microsoft.Office` or `Interop` (grep returns 0 matches) — zero Excel/COM references per phase goal.
  </acceptance_criteria>
  <done>`dotnet build src/FinanceFmtTools.sln -c Release` builds both the `net48` and `net8.0` outputs of `FinanceFmtTools.Engine` with 0 warnings/0 errors, and the solution file references the project.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap xUnit test project and define shared contract types</name>
  <files>src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj, src/FinanceFmtTools.Engine/FormatKeys.cs, src/FinanceFmtTools.Engine/FormatCategory.cs, src/FinanceFmtTools.Engine/CellAlignment.cs, src/FinanceFmtTools.Engine/FormatDef.cs</files>
  <read_first>
    src/modConfig.bas (lines 19-29 — FMT_* constants, exact string values to port)
    src/modFormatEngine.bas (lines 9-16 — FormatDef Type; lines 81-170 — GetFormatDef, note in particular that no `Case` branch ever assigns `f.Alignment`, which downstream plans must account for)
    src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj (created in Task 1 — confirms TargetFrameworks/LangVersion already set)
    .planning/phases/01-format-engine-core/01-RESEARCH.md (Standard Stack — exact package versions; Anti-Patterns — avoid `record`/`init` on net48, fails with CS0518)
  </read_first>
  <action>
    Run `dotnet new xunit -n FinanceFmtTools.Engine.Tests -o src/FinanceFmtTools.Engine.Tests --framework net8.0`, then delete the generated `src/FinanceFmtTools.Engine.Tests/UnitTest1.cs`.
    Edit `src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj`: keep `<TargetFramework>net8.0</TargetFramework>` singular (do NOT multi-target this project — net48 test hosts cannot execute on Linux, per 01-RESEARCH.md Pitfall 5). Set `<IsPackable>false</IsPackable>` and `<Nullable>disable</Nullable>`. Pin exact package versions: `Microsoft.NET.Test.Sdk` 17.14.1, `xunit` 2.9.3, `xunit.runner.visualstudio` 3.1.5 (with `<IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>` and `<PrivateAssets>all</PrivateAssets>` on the visualstudio runner package, matching 01-RESEARCH.md Standard Stack).
    Add `<ItemGroup><ProjectReference Include="..\FinanceFmtTools.Engine\FinanceFmtTools.Engine.csproj" /></ItemGroup>` to the test csproj.
    Run `dotnet sln src/FinanceFmtTools.sln add src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj`.
    Create `src/FinanceFmtTools.Engine/FormatKeys.cs`: `public static class FormatKeys` with `public const string` fields named `Integer`, `Fin2D`, `Fin4D`, `Fin8D`, `Pct4D`, `Pct2D`, `SpreadBps`, `DateIso`, `DateBr`, `DateBrLong`, `Text`, with values copied verbatim from modConfig.bas: `"INTEGER"`, `"FIN_2D"`, `"FIN_4D"`, `"FIN_8D"`, `"PCT_4D"`, `"PCT_2D"`, `"SPREAD_BPS"`, `"DATE_ISO"`, `"DATE_BR"`, `"DATE_BR_LONG"`, `"TEXT"` respectively.
    Create `src/FinanceFmtTools.Engine/FormatCategory.cs`: `public enum FormatCategory { Numeric, Percent, Date, Text }` — mirrors the 4 distinct string values VBA's `f.Category` ever holds (`"numeric"`, `"percent"`, `"date"`, `"text"`), as a type-safe enum instead of magic strings.
    Create `src/FinanceFmtTools.Engine/CellAlignment.cs`: `public enum CellAlignment { General, Left, Right }` — a COM-free stand-in for VBA's `XlHAlign`; Phase 2/3 will map this to the real `XlHAlign` enum when wiring real Excel COM objects.
    Create `src/FinanceFmtTools.Engine/FormatDef.cs`: `public sealed class FormatDef` with get-only auto-properties `string Key`, `string DisplayName`, `string NumberFormat`, `FormatCategory Category`, `CellAlignment Alignment`, and a public constructor `FormatDef(string key, string displayName, string numberFormat, FormatCategory category, CellAlignment alignment)` assigning all five parameters to the corresponding properties. Do NOT use a C# `record` or `init`-only properties — 01-RESEARCH.md confirms this fails to compile on `net48` with `CS0518`.
    Rebuild and run the (currently empty) test project to confirm the harness executes cleanly: `dotnet build src/FinanceFmtTools.sln -c Release` then `dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj -c Release`.
  </action>
  <verify>
    <automated>dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/FinanceFmtTools.sln -c Release` exits 0 with "0 Warning(s)" and "0 Error(s)".
    - `dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj -c Release` exits 0.
    - `src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj` contains `<TargetFramework>net8.0</TargetFramework>` (singular — no `TargetFrameworks` plural key in this file).
    - `src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj` contains a `ProjectReference` whose `Include` path ends in `FinanceFmtTools.Engine.csproj`.
    - `grep -v '^//' src/FinanceFmtTools.Engine/FormatKeys.cs | grep -c "public const string"` returns `11`.
    - `grep -c '"FIN_2D"' src/FinanceFmtTools.Engine/FormatKeys.cs` returns at least `1` (spot-check one constant value matches modConfig.bas verbatim).
    - `src/FinanceFmtTools.Engine/FormatDef.cs` does not contain the token `record` and does not contain `init;`.
    - `src/FinanceFmtTools.Engine/CellAlignment.cs` contains all three members `General`, `Left`, `Right`.
  </acceptance_criteria>
  <done>Both projects are in the solution, `dotnet build`/`dotnet test` succeed with zero warnings/errors, and the four shared contract files (`FormatKeys`, `FormatCategory`, `CellAlignment`, `FormatDef`) exist with the exact members specified above, ready for 01-02 and 01-03 to implement logic against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| NuGet package restore | `dotnet restore`/`dotnet build` pulls 4 packages (`Microsoft.NETFramework.ReferenceAssemblies`, `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`) from nuget.org into the build — the only external input this plan introduces |
| VBA source → C# port | The `FormatKeys`/`FormatDef` string/shape values are transcribed by hand from `src/modConfig.bas`/`src/modFormatEngine.bas` — a transcription error here silently propagates into every later plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-01-SC | Tampering | NuGet restore (`Microsoft.NETFramework.ReferenceAssemblies` 1.0.3, `xunit` 2.9.3, `xunit.runner.visualstudio` 3.1.5, `Microsoft.NET.Test.Sdk` 17.14.1) | accept | All four packages already verified in 01-RESEARCH.md's Package Legitimacy Audit via official NuGet registry cross-check + empirical `dotnet restore`/`build`/`test` execution this research session; long-established Microsoft/xunit.net-owned packages with no `[ASSUMED]`/`[SUS]` verdict — no blocking human-verify checkpoint required per the audit's own conclusion. |
| T-01-02 | Tampering (transcription) | `FormatKeys.cs` string constant values | mitigate | Task 2's acceptance criteria grep-verifies constant count (11) and spot-checks at least one literal value (`FIN_2D`) against `modConfig.bas`; full parity is exhaustively proven by 01-03's registry tests, which assert every `FormatDef.NumberFormat` string byte-for-byte against the VBA source. |

</threat_model>

<verification>
`dotnet build src/FinanceFmtTools.sln -c Release` succeeds with 0 warnings/0 errors across both `net48` and `net8.0` legs of `FinanceFmtTools.Engine`, and `dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj -c Release` runs cleanly (0 tests is expected at this stage).
</verification>

<success_criteria>
- Solution and both projects exist and are wired together (`dotnet sln list` shows both `.csproj` files).
- `FinanceFmtTools.Engine` multi-targets `net48;net8.0`; `FinanceFmtTools.Engine.Tests` targets `net8.0` only.
- `FormatKeys`, `FormatCategory`, `CellAlignment`, `FormatDef` exist with the exact members specified in this plan, zero Excel/COM references anywhere in `FinanceFmtTools.Engine`.
- DEV-01 satisfied: build and (empty) test run complete 100% via `dotnet` CLI, no Visual Studio required.
</success_criteria>

<output>
Create `.planning/phases/01-format-engine-core/01-01-SUMMARY.md` when done
</output>
