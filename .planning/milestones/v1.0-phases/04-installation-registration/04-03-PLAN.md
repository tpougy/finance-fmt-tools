---
phase: 04-installation-registration
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [INST-01, INST-02, INST-03]

must_haves:
  truths:
    - "[human_needed] A non-admin user can run scripts/install.ps1 -Source <locally-built bin folder> on a real Windows+Excel machine and see the 'Finance Fmt' Ribbon tab after opening Excel, with zero admin prompts"
    - "[human_needed] Running scripts/install.ps1 a second time immediately after a successful install completes without error (idempotency)"
    - "[human_needed] Running scripts/uninstall.ps1 removes the Ribbon tab after Excel restarts, and running it a second time completes without error (idempotency)"
    - "[human_needed] Deliberately triggering a slow/crashing add-in load once, then relaunching Excel, does NOT show Excel's native 'this add-in was disabled' notification — confirming DoNotDisableAddinList is behaviorally honored, not just written (04-RESEARCH.md Pitfall 2 / Open Question 1)"
  artifacts: []
  key_links: []
---

<objective>
Run Plan 01's `scripts/install.ps1` and Plan 02's `scripts/uninstall.ps1` end-to-end on a real Windows+Excel machine, using the local-testing escape hatch (`-Source`) against a locally `dotnet build`-produced `bin\` folder, and confirm the full INST-01/INST-02/INST-03 behavioral contract — something no amount of static/grep-based review in this Linux/WSL environment can prove.

Purpose: Plans 01/02 proved the scripts are structurally correct (identity values, registry-key shapes, idempotency logic) via grep-based static checks. This plan closes the gap between "the script is written correctly" and "the script actually works" — registration, Ribbon-tab appearance, idempotent re-runs, and the one genuinely uncertain behavior this phase's research flagged: whether `DoNotDisableAddinList` is honored by Excel the way it is documented for Outlook (04-RESEARCH.md Pitfall 2).

Output: A recorded pass/fail result for the itemized checklist below, captured in this plan's `SUMMARY.md` exactly as Phase 3's 03-02-SUMMARY.md recorded its own live-Excel smoke test — `human_needed`, not faked, not skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-installation-registration/04-CONTEXT.md
@.planning/phases/04-installation-registration/04-RESEARCH.md
@.planning/phases/03-com-entry-point-real-excel-integration/03-02-SUMMARY.md

@scripts/install.ps1
@scripts/uninstall.ps1
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live install/uninstall/idempotency/Resiliency verification on real Windows+Excel</name>
  <files>N/A — verification-only task; no files modified by this task.</files>
  <read_first>
    scripts/install.ps1, scripts/uninstall.ps1 (Plan 01/02 outputs — the exact scripts being exercised), .planning/phases/04-installation-registration/04-RESEARCH.md (Pitfall 2 / Open Question 1 — the Resiliency behavioral caveat this task specifically resolves), .planning/phases/03-com-entry-point-real-excel-integration/03-02-SUMMARY.md (precedent for how a live-Excel human_needed checklist result gets recorded)
  </read_first>
  <action>
    Run `dotnet build src/FinanceFmtTools.sln -c Release` as a precondition check (confirms the binaries steps 3-8 depend on actually exist and build clean), then present the itemized live-Excel checklist below (fresh install via the `-Source` escape hatch, Ribbon tab appearance, idempotent re-install, the Resiliency behavioral test, uninstall, idempotent uninstall) to the user, and block on their explicit pass/fail response — none of steps 3-8 is executable in this Linux/WSL environment, per 04-CONTEXT.md's non-discretionary environment constraint.
  </action>
  <what-built>
    Plan 01 built `scripts/install.ps1` (GitHub-Releases one-liner install + `-Package`/`-Source` local-testing escape hatch + 3-registry-tree HKCU registration: COM class, Excel discovery `LoadBehavior=3`, Resiliency `DoNotDisableAddinList=1`). Plan 02 built `scripts/uninstall.ps1` (idempotent removal of the same 3 trees plus installed files) and `scripts/verify-environment.ps1` (read-only diagnostic). All structural/identity correctness was verified via grep-based static checks in this Linux/WSL environment (no Windows/Excel/PowerShell available here) — this task is the first point where the scripts actually run.
  </what-built>
  <how-to-verify>
    On a Windows machine with Excel 2016+ installed and this repo cloned:

    1. Build the add-in locally: run `dotnet build src\FinanceFmtTools.sln -c Release` from the repo root. Confirm `src\FinanceFmtTools.ComAddin\bin\Release\net48\` contains `FinanceFmtTools.ComAddin.dll`, `FinanceFmtTools.Engine.dll`, `Microsoft.Office.Interop.Excel.dll`, and `office.dll`.

    2. (Optional but recommended) Run `powershell -ExecutionPolicy Bypass -File .\scripts\verify-environment.ps1 -RuntimeOnly` and confirm it reports `.NET Framework 4.8` and `Excel Classic` as `[OK]`, exiting 0.

    3. FRESH INSTALL (INST-01, local-testing escape hatch): close Excel if open, then run `powershell -ExecutionPolicy Bypass -File .\scripts\install.ps1 -Source src\FinanceFmtTools.ComAddin\bin\Release\net48`. Confirm the script reports `[OK]` for every registry key/file check in its post-install validation section and exits 0. Confirm no admin/UAC prompt ever appeared (every write should target `HKCU`).

    4. RIBBON TAB APPEARS (INST-01): open Excel. Confirm the "Finance Fmt" tab appears with the same groups/buttons/tooltips as Phase 3's smoke test already confirmed (Numérico/Percentual/Data/Texto/Info). Close Excel.

    5. IDEMPOTENT RE-INSTALL (roadmap criterion #4): with Excel closed, run the exact same `install.ps1 -Source ...` command again. Confirm it completes without error (exit 0), does not prompt about "already exists," and the Ribbon tab still appears correctly on the next Excel launch.

    6. RESILIENCY BEHAVIOR TEST (INST-03, the one item static review cannot prove — 04-RESEARCH.md Pitfall 2 / Open Question 1): with the add-in installed, deliberately force a slow or crashing load once (for example, temporarily rename `FinanceFmtTools.Engine.dll` so `FinanceFmtTools.ComAddin.dll` fails to load, or introduce a temporary unhandled exception in `Connect.OnConnection`, then restore it after this check). Launch Excel with the broken DLL in place, let it fail/crash once, then restore the correct DLL and relaunch Excel again. Confirm Excel's next launch does NOT show its native "we noticed you're having a problem, this add-in has been disabled" notification, and the Ribbon tab still loads normally — this is the actual behavioral proof that `DoNotDisableAddinList` is honored for Excel, not just a documentation-corroborated guess.

    7. UNINSTALL (INST-02): close Excel, run `powershell -ExecutionPolicy Bypass -File .\scripts\uninstall.ps1`. Confirm it reports removal of all 3 registry trees and the installed files, and exits 0. Open Excel and confirm the "Finance Fmt" tab no longer appears.

    8. IDEMPOTENT UNINSTALL (roadmap criterion #4): with the add-in already uninstalled, run `uninstall.ps1` again. Confirm it reports "already absent" for every key/file and still exits 0 (no error).

    Report back which of steps 3-8 passed and which (if any) failed, with enough detail (exact error text, which step) to file a gap-closure plan if needed.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks (steps 3-8) passed, or describe exactly which step failed and how (error text, unexpected registry state, notification that did/didn't appear).</resume-signal>
  <verify>
    <automated>dotnet build src/FinanceFmtTools.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    A precondition automated check must already be green before this task is reached: `dotnet build src/FinanceFmtTools.sln -c Release` exits 0 with 0 Warnings/0 Errors (proven in Phase 3, unaffected by Plans 01/02 which touch only `scripts/*.ps1`). This task's own acceptance is the human's explicit response to every itemized line in `how-to-verify` (steps 3-8) — "approved"/"issues found" is not itself machine-verifiable in this Linux/WSL environment, per 04-CONTEXT.md's non-discretionary environment constraint.
  </acceptance_criteria>
  <done>User has responded "approved" (all 6 checklist items in steps 3-8 passed) or has itemized which specific step(s) failed and how.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human executor -> real Windows+Excel machine | This task's entire purpose is to cross the boundary this environment cannot cross (Linux/WSL has no Windows/Excel/registry) |
| Deliberately-broken DLL (step 6) -> installed add-in | Intentional, temporary, human-controlled fault injection to test Resiliency behavior — must be reverted after the check, not left in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Step 6's deliberate DLL corruption/rename | accept | Explicitly temporary and human-supervised; the checklist requires restoring the correct DLL immediately after observing the Resiliency behavior, before resuming normal use |
| T-04-SC | Tampering | npm/pip/cargo/NuGet installs | accept | Not applicable — no new packages introduced anywhere in this phase |
</threat_model>

<verification>
`dotnet build src/FinanceFmtTools.sln -c Release` is the only automatable precondition check for this task in this Linux/WSL environment (confirms the binaries steps 3-8 depend on exist and build clean). Beyond that, this entire plan IS the verification step for Plans 01/02's static-review-only output — there is no separate automated check for steps 3-8 themselves; this environment cannot run PowerShell, Excel, or touch the Windows registry.
</verification>

<success_criteria>
All 6 checklist items (steps 3-8: fresh install, Ribbon tab appears, idempotent re-install, Resiliency behavior test, uninstall, idempotent uninstall) are confirmed passing by the human executor on a real Windows+Excel machine, closing out INST-01, INST-02, and INST-03 as fully verified (not just statically reviewed). Any failure is recorded with enough detail to drive a gap-closure plan (`/gsd-plan-phase 04 --gaps`).
</success_criteria>

<output>
Create `.planning/phases/04-installation-registration/04-03-SUMMARY.md` when done, recording the human executor's reported pass/fail result for each of the 6 checklist items verbatim (matching the precedent set by `.planning/phases/03-com-entry-point-real-excel-integration/03-02-SUMMARY.md`'s "User Setup Required" section).
</output>
