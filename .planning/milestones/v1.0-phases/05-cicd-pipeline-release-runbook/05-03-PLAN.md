---
phase: 05-cicd-pipeline-release-runbook
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/ThisWorkbook.bas, src/modConfig.bas, src/modFormatEngine.bas, src/modRibbon.bas, src/modUtils.bas, Install-FinanceFmtTools.ps1, Install-FinanceFmtTools.bat, README.md]
autonomous: true
requirements: [LEGACY-01, LEGACY-02]

must_haves:
  truths:
    - "The VBA source (.bas files) and legacy root PowerShell/.bat installer no longer exist on main's working tree, while remaining fully recoverable from the archive/vba-legacy branch"
    - "src/customUI14.xml still exists on main unmodified, since FinanceFmtTools.Engine.csproj actively embeds it at build time"
    - "dotnet build still succeeds with 0 Warning(s)/0 Error(s) after the VBA files are removed, proving customUI14.xml survived and no C# project referenced a removed file"
    - "README.md documents only the new C# add-in's install/uninstall flow, with the old VBA .xlam flow fully absent from the active instructions"
  artifacts:
    - path: "README.md"
      provides: "C#-only user-facing documentation (LEGACY-02)"
      contains: "scripts/install.ps1"
    - path: "src/customUI14.xml"
      provides: "Ribbon XML still embedded by the C# build — must survive LEGACY-01's removal untouched"
  key_links:
    - from: "README.md"
      to: "scripts/install.ps1"
      via: "documented one-liner install command"
      pattern: "scripts/install\\.ps1"
    - from: "src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj"
      to: "src/customUI14.xml"
      via: "EmbeddedResource Link (pre-existing, untouched by this plan)"
      pattern: "EmbeddedResource Include=\"\\.\\./customUI14\\.xml\""
---

<objective>
Remove the retired VBA source and legacy root installer scripts from `main`'s active working tree
(LEGACY-01), while confirming they remain fully recoverable on the already-existing local
`archive/vba-legacy` branch, and rewrite `README.md` so it documents only the new C# add-in's
install/uninstall/architecture (LEGACY-02).

Purpose: Closes both remaining non-CI/non-runbook requirements of Phase 5 — the milestone's own goal
states the VBA legacy code/docs must be "completely out of the active main flow." This plan performs
only local git operations (no push) and a local file rewrite.
Output: `main`'s working tree no longer contains `src/*.bas` or the two legacy root installer files;
`README.md` rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cicd-pipeline-release-runbook/05-RESEARCH.md
@.planning/phases/05-cicd-pipeline-release-runbook/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive VBA source off main (LEGACY-01)</name>
  <files>src/ThisWorkbook.bas, src/modConfig.bas, src/modFormatEngine.bas, src/modRibbon.bas, src/modUtils.bas, Install-FinanceFmtTools.ps1, Install-FinanceFmtTools.bat</files>
  <read_first>
    .planning/phases/05-cicd-pipeline-release-runbook/05-RESEARCH.md (Pitfall 1 — "Deleting
    src/customUI14.xml along with the .bas files"; "Archiving the VBA branch" code example, lines
    ~362-376; Recommended Project Structure, lines ~146-169).
    src/FinanceFmtTools.Engine/FinanceFmtTools.Engine.csproj (confirms the
    `<EmbeddedResource Include="../customUI14.xml" Link="Resources/customUI14.xml" />` line — this file
    must NOT be touched by this task).
  </read_first>
  <action>
    First, verify (read-only, no mutation) that the local `archive/vba-legacy` branch already has a
    complete pre-migration snapshot: run `git log archive/vba-legacy -1 --format='%H %s'` and confirm
    the tip commit is `cf2559b` ("chore: configure GSD model profile and YOLO mode", the last commit
    before the C# migration started, per 05-RESEARCH.md), then run
    `git ls-tree -r archive/vba-legacy --name-only -- src` and confirm the output is exactly these 6
    paths: `src/ThisWorkbook.bas`, `src/customUI14.xml`, `src/modConfig.bas`, `src/modFormatEngine.bas`,
    `src/modRibbon.bas`, `src/modUtils.bas`. Only after this passes, on the current `main` branch, run
    `git rm src/ThisWorkbook.bas src/modConfig.bas src/modFormatEngine.bas src/modRibbon.bas
    src/modUtils.bas Install-FinanceFmtTools.ps1 Install-FinanceFmtTools.bat` — do NOT include
    `src/customUI14.xml` in this list (Pitfall 1: it is still an active `EmbeddedResource` for
    `FinanceFmtTools.Engine.csproj`, and deleting it breaks `dotnet build` for every already-shipped
    phase). Commit the removal locally on `main` only, with a message equivalent to
    `chore(legacy): remove retired VBA source and legacy installer from main (LEGACY-01)`, noting in the
    commit body that the removed content is preserved in full on `archive/vba-legacy` (already forked
    from commit `cf2559b` before this migration started). Do NOT run `git push` for either `main` or
    `archive/vba-legacy` in this task — pushing either branch to `origin` is explicitly deferred to this
    phase's final human-authorization checkpoint plan (05-04-PLAN.md); every git operation in this task
    is 100% local.
  </action>
  <acceptance_criteria>
    `git log archive/vba-legacy -1 --format=%H` starts with `cf2559b`. `git ls-tree -r archive/vba-legacy
    --name-only -- src | sort` equals the sorted 6-path list above. After the commit, `git status
    --short` on `main` shows a clean working tree. `src/*.bas` matches zero files on `main`.
    `src/customUI14.xml` still exists. `Install-FinanceFmtTools.ps1` and `Install-FinanceFmtTools.bat`
    no longer exist. `dotnet build src/FinanceFmtTools.sln -c Release` completes with `0 Warning(s)` and
    `0 Error(s)` (baseline re-confirmed in this session before this plan was written).
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; test "$(git log archive/vba-legacy -1 --format=%H | cut -c1-7)" = "cf2559b" && diff <(git ls-tree -r archive/vba-legacy --name-only -- src | sort) <(printf '%s\n' src/ThisWorkbook.bas src/customUI14.xml src/modConfig.bas src/modFormatEngine.bas src/modRibbon.bas src/modUtils.bas | sort) && test -z "$(git status --short)" && ! ls src/*.bas >/dev/null 2>&1 && test -f src/customUI14.xml && test ! -f Install-FinanceFmtTools.ps1 && test ! -f Install-FinanceFmtTools.bat && BUILD_OUT="$(dotnet build src/FinanceFmtTools.sln -c Release 2>&1)" && echo "$BUILD_OUT" | grep -qE '0 Warning\(s\)' && echo "$BUILD_OUT" | grep -qE '0 Error\(s\)' && echo ALL_PASS</automated>
  </verify>
  <done>
    `main`'s working tree no longer contains the 5 `.bas` files or the 2 legacy installer scripts,
    `src/customUI14.xml` is untouched and still builds successfully, and the removal is captured in
    exactly one new local commit on `main` — nothing pushed to `origin`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for the C# add-in only (LEGACY-02)</name>
  <files>README.md</files>
  <read_first>
    README.md (current full file — the "Família Fin xD" and "Outros formatos" format tables and the
    "Referência rápida do ribbon" tree are user-facing and stay accurate per 05-RESEARCH.md; everything
    else needs rewriting).
    .planning/phases/05-cicd-pipeline-release-runbook/05-RESEARCH.md (Runtime State Inventory
    "OS-registered state" row — the recommended "upgrading from VBA" note; State of the Art table).
    scripts/install.ps1, scripts/uninstall.ps1, scripts/verify-environment.ps1 (`.EXAMPLE`/doc-comment
    blocks — the exact one-liners and flags to quote).
  </read_first>
  <action>
    Rewrite `README.md`, keeping the existing user-facing format tables (the "Família Fin xD", "Outros
    formatos" percentual/spread/date/text sections, and the "Referência rápida do ribbon" tree)
    verbatim — 05-RESEARCH.md confirms these remain accurate for the C# add-in. Replace every other
    section: (1) top header — remove the hardcoded `**Versão:** 1.0.0` line (versions are now tracked
    via GitHub Release tags, not a static README claim) and update the platform line to mention "C#
    (.NET Framework 4.8) COM add-in"; (2) rewrite "Instalação" to show the new one-liner exactly as
    documented in `scripts/install.ps1`'s own `.EXAMPLE`
    (`Set-ExecutionPolicy Bypass -Scope Process -Force; irm
    https://raw.githubusercontent.com/tpougy/finance-fmt-tools/main/scripts/install.ps1 | iex`), state
    the real requirements (Windows + Excel 2016+ 64-bit, .NET Framework 4.8+, no admin required),
    mention `scripts/uninstall.ps1` for removal and `scripts/verify-environment.ps1 -RuntimeOnly` as an
    optional pre-install diagnostic, and add a short "Atualizando da versão VBA" callout instructing
    anyone with the old `.xlam` installed to remove it first (Excel > Arquivo > Opções > Suplementos >
    Gerenciar > Suplementos COM, or delete the file from `%APPDATA%\Microsoft\AddIns`) to avoid two
    "Finance Fmt" ribbon tabs appearing simultaneously; (3) delete the entire "Persistência de
    configurações" section describing `CustomXMLPart` — replace it with a short paragraph stating the
    two checkboxes ("Alinhar à direita", "Zero contábil") are session-only in the C# version and always
    reset to their defaults (off/on) when Excel restarts, with no persistence mechanism; (4) rewrite
    "Arquitetura do projeto" to describe the actual C# solution layout (`src/FinanceFmtTools.Engine` —
    format engine + abstractions, `src/FinanceFmtTools.ComAddin` — COM entry point `Connect.cs` /
    `AddInHost.cs`, `src/FinanceFmtTools.Engine.Tests` — xUnit tests, `src/customUI14.xml` — Ribbon XML
    embedded by `FinanceFmtTools.Engine.csproj`) instead of the removed `.bas` modules, and mention the
    VBA source's continued existence on the `archive/vba-legacy` branch for historical reference; (5)
    add a brief "Desenvolvimento" section documenting `dotnet build src/FinanceFmtTools.sln` and
    `dotnet test src/FinanceFmtTools.Engine.Tests/FinanceFmtTools.Engine.Tests.csproj`, no Visual Studio
    required. Do not delete the format tables or the ribbon-tree reference.
  </action>
  <acceptance_criteria>
    `README.md` contains `scripts/install.ps1`; does not contain `Install-FinanceFmtTools.ps1`; does not
    contain `CustomXMLPart` (case-insensitive); does not contain any of `modFormatEngine.bas`,
    `modRibbon.bas`, `modUtils.bas`, `modConfig.bas`, `ThisWorkbook.bas`; still contains `Fin 2D` and
    `0.00%` (format tables preserved); contains `archive/vba-legacy` (historical reference retained).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'scripts/install.ps1' README.md && ! grep -q 'Install-FinanceFmtTools.ps1' README.md && ! grep -qi 'CustomXMLPart' README.md && ! grep -qE 'modFormatEngine\.bas|modRibbon\.bas|modUtils\.bas|modConfig\.bas|ThisWorkbook\.bas' README.md && grep -q 'Fin 2D' README.md && grep -q '0.00%' README.md && grep -q 'archive/vba-legacy' README.md && echo ALL_PASS</automated>
  </verify>
  <done>
    `README.md` documents only the C# add-in's install/uninstall/architecture, the format tables remain
    intact and accurate, and any mention of the old VBA flow is limited to a single explicit "upgrading
    from VBA" migration note (no old `CustomXMLPart` persistence claims, no old installer script name).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local git repository (`main` vs. `archive/vba-legacy`) | Purely local, single-developer trust boundary — no network/remote action in this plan. |
| `README.md` -> end user following install instructions | Documentation consumed by humans installing the add-in on their own Windows/Excel machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | `git rm` targeting the wrong file set (e.g., accidentally including `src/customUI14.xml`) | mitigate | Task 1's action explicitly excludes `customUI14.xml` from the `git rm` file list, and its acceptance criteria assert both its continued presence AND a successful `dotnet build` afterward (05-RESEARCH.md Pitfall 1's own documented warning sign). |
| T-05-08 | Repudiation | History rewrite / force-push that could make the VBA removal unrecoverable | accept | Explicitly avoided by design — plain `git rm` + a normal commit, zero history rewriting; full recovery always available via `archive/vba-legacy` (05-RESEARCH.md Anti-Patterns: "Force-pushing or rewriting history ... unnecessary and destructive"). |
| T-05-09 | Tampering | README's "upgrading from VBA" instructions telling users to delete a file/registry entry could mislead a user into removing something unrelated if worded imprecisely | mitigate | Task 2's action specifies the exact, narrow removal path (`%APPDATA%\Microsoft\AddIns`, Excel's own native "Suplementos COM" manager UI) — matches the legacy `.xlam`'s actual, previously-documented installation location, not a guess. |

</threat_model>

<verification>
Run both tasks' automated verify commands. Confirm `git log -1 --oneline` on `main` shows the new
`chore(legacy): ...` commit and `git status` reports a clean tree with nothing pushed to `origin`.
</verification>

<success_criteria>
- `main`'s working tree has zero `src/*.bas` files and no root-level legacy installer scripts.
- `src/customUI14.xml` is unchanged and `dotnet build` remains green (0 Warning(s)/0 Error(s)).
- `README.md` passes every acceptance_criteria check in Task 2.
- Exactly one new local commit exists on `main`; nothing pushed to `origin`.
</success_criteria>

<output>
Create `.planning/phases/05-cicd-pipeline-release-runbook/05-03-SUMMARY.md` when done.
</output>
